#!/usr/bin/env node

const request = require("request");

request.post(
    "http://newsapi.xyz/update/" + process.env.CODE,
    function (error, response, body) {
        if (!error) {
            console.log(body)
        }
    }
);